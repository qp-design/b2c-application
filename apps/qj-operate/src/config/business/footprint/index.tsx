import { FieldType } from '@brushes/form';
import React from 'react';
import { SelectColor } from '@/common';

export const formConfig: Array<FieldType> = [];

export const title = '足迹';
export const info = '设置足迹的样式和配置';
